{
  "private": true,
  "scripts": {
    "deploy": "surge ./public",
    "start": "browser-sync start --server 'public' --files 'public'"
  },
  "devDependencies": {
    "browser-sync": "^2.17.5",
    "stylelint": "^7.5.0",
    "stylelint-config-standard": "^14.0.0"
  }
}
